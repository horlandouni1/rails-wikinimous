<div class="d-flex justify-content-between">
    <h1>Articulos</h1>
    <h3><%= link_to 'agregar article', new_article_path %></h3>
</div>
<% @articles.each do |article| %>
<h3 class="fs-1"><%= link_to article.title, article_path(article) %></h3>
<p class="fs-2"><%= article.content %></p>
<% end %>